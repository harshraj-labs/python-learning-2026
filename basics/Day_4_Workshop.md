Today I was introduced with a workshop to practice what I've been previously taught.
The workshop consists of making a report card printer, using variables like: name, age, score, is_student (to check if the student is enrolloed or not);
also to show what is the datatype of variables.

The code was as follows:
```
name = 'Alice'
print(name, type(name))
age = 20
print(age, type(age))
is_student = True
print(is_student, type(is_student))
score =80.5
print(isinstance(score, int)
print(score,type(score))
```
**OUTPUT**
```
Alice <class = 'str'>
20 <class = 'int'>
True <class = 'boolen'>
False
80.5 <class = 'float'>
``

That's it! and for a change today I thought of typing this day's progress on github directly by creating a file here and commiting my changes,
usually I use VS code to write these .md files and push it to the repo.
